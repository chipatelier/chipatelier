// ChipAtelier frontend — placeholder root component.
// Will be replaced with full UI in plan 01-02.
import React from "react";

function App(): React.ReactElement {
  return (
    <div style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>ChipAtelier</h1>
      <p>RTL-to-GDS ASIC implementation platform — loading...</p>
    </div>
  );
}

export default App;
